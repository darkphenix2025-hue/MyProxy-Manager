#!/bin/sh
set -eu

repository_root=$(git rev-parse --show-toplevel)
cd "$repository_root"

if [ "${SKIP_LOCAL_CI:-}" = "1" ]; then
  echo "Local pre-commit checks skipped (SKIP_LOCAL_CI=1)."
  exit 0
fi

npm run ci:local
